# WatchDog 火场安全管控 App UI 设计规范

版本：V0.2

对应 App：`0.8.0+17`

适用范围：Android 优先的消防救援现场安全管控 Flutter 应用
状态：已与现行 Flutter 主题、导航、语音流程、日志和智能辅助实现对齐；V0.1 保留为历史基线。

## 1. 设计定位

### 1.1 产品任务

WatchDog 帮助安全员通过语音完成进场、出场、日志和现场问答，并在同一任务码下同步人员、气瓶倒计时和操作记录。界面必须让安全员在一至三秒内回答：

1. 当前谁在场；
2. 谁需要立即关注；
3. 最早何时到期，以及下一步该做什么。

### 1.2 视觉方向

采用“高可视亮色工业监控”方向：浅灰页面背景、白色工作表面、近黑文字和深色线性图标。颜色只承担明确语义：橙色代表语音动作，绿色/黄色/红色/深红代表安全分级。视觉应直接、克制、可恢复；不使用品牌 Logo、渐变、玻璃拟态、霓虹、长投影或装饰性插画。

### 1.3 设计原则

1. 先危险、后操作：报警、超时、最早到期始终先于普通信息出现。
2. 状态冗余：不能只靠颜色，必须同时使用短文案、图标、排序、位置和倒计时。
3. 戴手套可用：关键目标不小于 48dp，语音与确认动作不依赖精细手势。
4. 现场连续：同步、断网、识别失败、无数据和任务结束都保留原因与恢复路径。
5. 一次只给一个主动作：看板看态势、任务页做语音确认、辅助页做提问；不在同一区域堆叠相同优先级的按钮。
6. 代码是规范的一部分：颜色、圆角、间距、阴影和状态名称复用 `AppColors`、`AppRadius`、`AppSpacing`、`AppShadow`、`EntryStatus`，不得在页面内另建视觉体系。

## 2. 信息架构与导航

### 2.1 一级入口

App 启动默认进入看板。一级导航固定为五个等距槽位，顺序不得更改：

| 序号 | 入口 | 页面/模式 | 核心任务 |
|---:|---|---|---|
| 1 | 日志 | `NotesPage` | 查看、筛选、补充火场时间线 |
| 2 | 看板 | `BoardPage` | 监控在场人员、倒计时和危险状态 |
| 3 | 语音 | `HomePage` | 任务码管理、按住说话、识别与确认 |
| 4 | 辅助 | `ChatPage` | 向“水元素”文字或语音提问 |
| 5 | 设置 | `SettingsPage` | 配置服务、识别、提醒、保活与二级工具 |

`ChatPage` 是上下文模式：进入后，常规五入口替换为辅助专用的语音/文字输入条；顶部返回回到进入辅助前的入口，而不是固定跳回看板。

### 2.2 二级页面

| 入口 | 二级页面 | 目的 |
|---|---|---|
| 看板 | `EntryDetailPage` | 查看单人倒计时、气瓶信息并确认出场 |
| 看板 | `ReportPressureSheet` | 更新在场人员当前压力 |
| 设置 | `RosterPage` | 管理消防员名单、专业术语和批量导入 |
| 设置 | `StatsPage` | 查看出场次数、时长和压力统计 |
| 设置 | `OpLogPage` | 按一次语音操作查看全链路记录并同步 |
| 设置 | `AboutPage` | 查看项目说明与使用方式 |

### 2.3 语音意图路由

| 识别意图 | 页面行为 |
|---|---|
| 进场 / 出场 | 停留或转入任务页，展示可编辑的确认面板；出场动作须二次确认或使用已确认流程 |
| 日志 | 自动记入火场日志并跳转日志页 |
| 提问 | 跳转辅助页并自动发送 |
| 无关 / 未识别 | 丢弃本次结果，清楚提示重新录入，不切换页面 |

辅助页内的语音同样遵循此路由：只有“提问”留在本页发送，其他意图进入对应业务页面。

## 3. 视觉基础

### 3.1 颜色

下表与 `app/lib/theme/app_widgets.dart` 的 `AppColors` 一致。页面不应新增未经定义的业务色。

| Token | 色值 | 用途 |
|---|---|---|
| `background` | `#F2F4F7` | 页面主背景 |
| `surface` | `#FFFFFF` | 卡片、底栏、输入和弹窗 |
| `surfaceSubtle` | `#E8ECF1` | 中性容器、加载/禁用背景 |
| `border` | `#D7DDE5` | 1dp 分隔线、输入边框 |
| `textPrimary` | `#111111` | 主标题、数值、选中导航、主要按钮 |
| `textSecondary` | `#4B5563` | 辅助正文、字段值 |
| `textTertiary` | `#6B7280` | 低优先级标签、未选中导航、处理中 |
| `safe` | `#32C943` | 安全状态 |
| `caution` | `#FFB000` | 注意状态 |
| `alarm` | `#F5222D` | 报警状态、危险确认 |
| `timeout` | `#C90016` | 超时状态 |
| `voice` | `#FF9500` | 语音按钮和语音相关动作 |
| `online` | `#20A83A` | 已连接状态 |

规则：

- 橙色不表示报警；红色不表示普通主按钮。普通确认动作使用近黑色。
- 报警与超时卡片使用白色前景；安全与注意卡片使用近黑色前景。
- 透明色只能从表中颜色派生，用于低强度背景、边缘或阴影，不能形成新的状态含义。
- 处理态使用中性灰，避免与录音橙色混淆。

### 3.2 字体与数字

中文界面使用 `PingFang SC, Noto Sans CJK SC, Microsoft YaHei, sans-serif`；数字使用 `DIN Alternate, SF Mono, Roboto Mono, ui-monospace, monospace`，并启用等宽数字，避免倒计时横向跳动。

| 样式 | 实现值 | 用途 |
|---|---:|---|
| `displayXl` | 56dp / 1.0 / 800 | 人员卡片核心倒计时 |
| `displayLg` | 44dp / 1.05 / 800 | 详情页倒计时 |
| `h1` | 24dp / 1.2 / 800 | 一级页面标题 |
| `h2` | 22dp / 1.25 / 700 | 人员名、模块标题 |
| `bodyLg` | 17dp / 1.4 / 600 | 主操作与状态文案 |
| `bodyMd` | 15dp / 1.45 / 500 | 常规正文 |
| `label` | 13dp / 1.35 / 600 | 字段标题、单位 |
| `caption` | 12dp / 1.4 / 500 | 同步信息、说明 |

底部导航标签是例外：使用 10.5dp / 1.1；未选中为 400，选中为 600。不得为制造层级而放大标签或使用背景胶囊。

### 3.3 尺寸、圆角与阴影

| 类别 | Token / 数值 | 用途 |
|---|---|---|
| 间距 | 4 / 8 / 12 / 16 / 20 / 24 / 32 / 40dp | 统一节奏；页面横向边距默认 16dp |
| 圆角 | 8 / 14 / 20 / 28 / 999dp | 小控件 / 卡片 / 重点卡 / 底部表面 / 胶囊 |
| 卡片阴影 | `0 2 8 rgba(17,17,17,.08)` | 白色表面与背景分离 |
| 浮层阴影 | `0 6 16 rgba(17,17,17,.16)` | 仅用于语音提示等短时浮层 |
| 普通触控 | 最小 48×48dp，推荐 56×56dp | 导航、图标按钮 |
| 关键触控 | 最小 56×56dp | 录音、确认出场、发送 |

## 4. 底部导航与语音按钮

### 4.1 五等分底部导航

底栏为白色表面，顶部 1dp 边界线，主体高度 68dp，额外叠加系统底部安全区。五个槽位的中心分别位于可用宽度的 10%、30%、50%、70%、90%；不要通过固定中央缺口或左右不同 `Spacer` 拼出视觉等距。

四个常规入口使用同一结构：

- 28dp 深色线性 `CustomPainter` 图标，图标整体上移 1dp；
- 图标与文字间距 2dp；文字在同一基线，标签不加背景；
- 最小命中高度 56dp，最小可点尺寸 48×48dp；
- 选中态仅使用 `textPrimary` 与 600 字重，未选中使用 `textTertiary` 与 400 字重；
- 过渡时长约 160ms，只改变颜色/字重，不改变槽位尺寸、文字基线或图标位置。

图标语义固定：日志为时间线与记录节点；看板为 2×2 数据格；辅助为正面防护机器人；设置为三条调节滑杆。它们使用统一 24×24 设计网格、1.8dp 圆头线宽；不直接把 PNG 作为导航背景。

### 4.2 中央语音按钮 `VoiceButton`

中央按钮处于第三槽位，视觉圆面为 64dp，命中容器为 72dp。视觉圆面必须完整收在 68dp 底栏背景内，上下至少保留约 2dp 安全余量；不能再以“悬浮出底栏”的方式制造层级。

| 状态 | 视觉 | 交互 |
|---|---|---|
| 默认 | `voice` 橙色圆面 + 白色麦克风 | 点击进入任务页；长按开始录音 |
| 按下 | 橙色加深，缩放至 0.94 | 不改变槽位、命中区或底栏高度 |
| 录音中 | 深橙色；麦克风置于上半部，底部显示三段白色短波形 | 松开结束；波形仅在按钮内部动画，不外扩 |
| 处理中 | `textTertiary` 中性圆面 + 白色转圈 | 禁止点击、长按和重复提交；语义提示“识别处理中” |
| 失败 / 无权限 | 回到可操作视觉面，并在页面内给出明确错误与重试/系统设置入口 | 不通过把按钮永久置灰来隐藏恢复路径 |

录音中的短波形是局部反馈，不使用大范围声波、扩散圆环或持续橙色光晕。录音状态提示“正在聆听，松开结束”只出现一次，页面和底栏不可重复播报同一提示。

### 4.3 辅助页专用底栏

进入辅助页后，底栏不显示五个导航图标：

- 默认语音态：中央使用同一 `VoiceButton`，左侧保留“文字”切换；
- 文字态：显示输入框与一个发送按钮，中间槽位显示“语音”切换，麦克风隐藏；
- 键盘出现时，输入条必须顶在键盘上方；发送中禁用发送按钮并显示转圈；
- 辅助专用输入条与常规底栏同样保留底部安全区和 1dp 顶部分隔线。

## 5. 核心组件

### 5.1 状态组件

`EntryStatus` 固定映射 `normal / warn / alarm / timeout` 到“安全 / 注意 / 报警 / 超时”。状态徽章包含文字和图标；人员列表按超时、报警、注意、安全排序，同级按剩余时间升序。

人员卡片最小高度 120dp，整卡可点击。姓名与状态在上层，倒计时是视觉重心（不小于 56dp），气瓶压力、时长上限与更新时间为次级信息。卡片尾部留出不少于 100dp 的滚动安全距离，避免被底栏遮挡。

### 5.2 看板概览

`SummaryBanner` 固定呈现“在场人员 / 需关注 / 最早到期”三项，白色圆角容器内使用细分隔线。最早到期按当前危险级别着色；同步中保留已有数据，断网时显示最后可用数据并提供重试，不能用空白骨架替换现场信息。

### 5.3 任务与语音确认

`HomePage` 是“任务”页，不只是录音页。任务码、在场概览、更换任务码和结束任务集中于此；录音后展示原始转写、意图、人员、压力和可用时间，允许在确认前编辑姓名、压力和单人气瓶容量。

- 进场压力为必填，范围提示与字段高亮必须明确；
- 多人录入支持继续追加、移除单人和统一确认；
- “全员离场”“确认进入火场”“确认出火场”属于高风险操作，必须使用明确动词与二次确认；
- “转为日志记录”是意图修正，不应丢弃原始文本；
- 录音、转写、解析、确认的状态在页面中顺序可追溯，处理中不得允许重复发起。

### 5.4 日志

日志页采用单列时间线。一级操作为“写日志”；按“全部”和既定火场分类筛选，筛选只改变内容，不改变底栏位置。空状态必须说明可以通过底部语音或手动记录创建日志。编辑、删除使用底部表单/二次确认，删除不得静默完成。

### 5.5 智能辅助与头像

辅助角色名称为“水元素”。欢迎页使用 56dp 头像，助手消息使用 30dp 头像；两处统一使用 `AssistantAvatar`，不直接使用带白底 PNG。

头像为透明底 `CustomPainter` 重绘：正面一体式防护头罩、横向深色目镜、双橙色传感器、低矮天线和下部呼吸防护模块。外层可使用白色圆形容器、边框和克制卡片阴影，内部不出现耳机、聊天气泡、火焰、消防车、牙齿状“嘴巴”或夸张卡通表情。

辅助页顶部操作区使用透明背景，只有返回与清空记录操作；页面底部保留安全提醒“水元素建议仅供参考，现场以指挥员口令和现行规程为准。”助手气泡可用 Markdown 展示结论、立即行动和注意事项，但危险场景不能被聊天视觉弱化。

### 5.6 设置与二级工具

设置按“服务端、气瓶参数、语音识别、提醒方式、名单与热词、统计、操作日志、关于”分组，可折叠但不隐藏关键状态。保存采用自动保存：保存中、已保存和保存失败必须可见；令牌默认隐藏；本地模型显示已就绪、下载中、未下载或失败，并提供对应恢复动作。

统计和操作日志为设置二级入口，使用实数/空状态，不以装饰图表填充未知数据。详情页、压力更新表单和名单批量导入均使用相同表面、边框、圆角与确认语义。

## 6. 连接、反馈与动效

### 6.1 连接状态

看板、日志和设置均显示连接状态：已连接、同步中、云端断开或同步失败。同步失败时保留本地数据、标注其时效，并提供“重试同步”；连接状态不应只藏在设置页。

### 6.2 动效与声音

- 数字倒计时只更新数字；等宽数字防止抖动。
- 报警与超时可以低频呼吸，系统减少动效时保留静态红色、文字、图标和倒计时。
- 语音按下和导航选中使用 110–160ms 的短过渡；不得引起布局跳动。
- TTS、报警音、通知和屏幕常亮由设置开关控制；声音关闭后，视觉报警仍完整存在。
- SnackBar 只用于短暂反馈，不能承载报警；删除、出场、结束任务等不可逆动作必须二次确认。

## 7. 可访问性与窄屏约束

1. 320、360、390、430dp 宽度下，底栏五个入口等距、无重叠、无横向溢出，文字基线不跳动。
2. 普通图标入口至少 48×48dp，实际导航高度 56dp；语音命中区 72dp、视觉圆面 64dp。
3. 所有高危状态同时具有文字、图标、排序和颜色；不以红绿区分作为唯一提示。
4. 每个可触达控件提供语义名称、选中状态和可执行动作；处理中/禁用态必须暴露为不可操作。
5. 强光和较大系统字体下，人员姓名、倒计时、状态标签与主要按钮不可截断；过长姓名采用省略并可在详情中完整查看。
6. 键盘、底部安全区和语音提示不能遮挡最后一条人员卡、最后一条日志或聊天内容。

## 8. 代码落地映射

| 设计域 | 唯一实现入口 |
|---|---|
| 颜色、间距、字级、圆角、阴影、通用卡片 | `app/lib/theme/app_widgets.dart` |
| Flutter Theme 与字体回退 | `app/lib/theme/app_theme.dart` |
| 五个导航图标 | `app/lib/theme/nav_icons.dart` |
| 语音状态控件 | `VoiceButton`（`app_widgets.dart`） |
| 辅助机器人头像 | `app/lib/theme/assistant_avatar.dart` |
| 一级导航与辅助输入模式 | `app/lib/main.dart` |
| 看板、任务、日志、辅助、设置 | `app/lib/pages/` |

新增页面先复用上述组件。若现有 token 不足，应先扩展主题层并补充本规范，再在页面中使用；禁止为单页直接复制颜色、阴影或导航逻辑。

## 9. 验收清单

### 9.1 视觉与布局

- [ ] 页面使用既定浅灰背景、白色表面、深色线性图标与克制阴影。
- [ ] 五个底部槽位等距；四个常规图标为 28dp、标签为 10.5dp；选中项没有灰色圆底。
- [ ] 64dp 麦克风圆面完整位于 68dp 底栏背景内，72dp 命中区不改变布局。
- [ ] 默认、按下、录音、处理四种语音状态在一眼内可区分；录音波形仅在按钮内部。
- [ ] “水元素”头像在 56dp 和 30dp 下均清晰、圆形裁切完整、无白色位图方底。
- [ ] 聊天页顶部没有横向白色 AppBar 条；返回与清空按钮直接位于页面背景。

### 9.2 功能与现场可用性

- [ ] 长按录音、松手识别、点击进入任务页、识别处理中防重复触发均有效。
- [ ] 进场、出场、日志、提问和无关语音按既定意图路由。
- [ ] 危险人员总是在安全人员之前；最后一项内容可滚动到语音/输入条之上。
- [ ] 断网、同步失败、空数据、权限不足和识别失败有可执行恢复路径。
- [ ] 320/360/390/430dp 与系统底部安全区下无横向溢出、碰撞或布局跳动。

## 10. 版本记录

| 版本 | 日期 | 说明 |
|---|---|---|
| V0.2 | 2026-08-08 | 依据 `0.8.0+17` 代码更新为五入口导航、语音四态、任务页、日志、智能辅助、统计/操作日志与 Android 现场可用性规范。 |
| V0.1 | 2026-08-02 | 高可视亮色方案与初始三入口信息架构。 |
