{
  "name": "watchdog-backend",
  "version": "0.1.0",
  "private": true,
  "description": "消防救援现场安全管控 - 语音录入与气瓶倒计时后端",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "NODE_ENV=test node --test \"test/*.test.js\"",
    "db:migrate": "node scripts/migrate-cloudbase.js"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "express": "^4.19.2",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@cloudbase/manager-node": "^5.6.5"
  }
}
