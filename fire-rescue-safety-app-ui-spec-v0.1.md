# 火场安全管控 App UI 设计规范

> 历史基线：本文件记录 V0.1 的三入口方案，现行规范请使用 [V0.2](fire-rescue-safety-app-ui-spec-v0.2.md)。

版本：V0.1
方案：方案 2——高可视亮色 High-Vis Light
适用范围：移动端消防救援现场安全监控 App
当前状态：视觉方向和核心页面已确定，气瓶可用时间的最终业务公式及实际终端尺寸待确认

## 1. 设计定位

### 1.1 产品定位

本产品用于消防救援现场的在场人员登记、气瓶使用时间监控、语音录入、状态提醒和配置管理。核心任务是在强光、紧张、戴手套和注意力有限的环境下，让安全员快速回答三个问题：

1. 当前有多少人在场；
2. 谁需要立即关注；
3. 哪一名人员最接近气瓶使用时限。

### 1.2 视觉定位

采用高可视亮色方向，借鉴机场信息屏、警用平板和工业监控终端的视觉语言：

- 浅灰白背景，减少环境反光下的视觉干扰；
- 纯黑或近黑色超大字号，优先保证远距离可读性；
- 使用绿色、橙黄色、红色整块状态卡片形成快速分级；
- 大圆角、大触控目标、少装饰、少阴影；
- 报警状态使用红色卡片和白色倒计时，必要时辅以呼吸闪烁；
- 不使用品牌 Logo、复杂插画、渐变背景或装饰性纹理。

### 1.3 设计原则

1. 安全优先：重要状态和危险信息必须先于装饰和次要信息被识别。
2. 一眼读懂：看板页面应在一秒至三秒内完成整体状态判断。
3. 大触控目标：主要操作适合戴手套完成，减少误触和连续精确点击。
4. 状态可冗余识别：不能只依靠颜色，还要同时使用文字、图标、位置和倒计时变化表达状态。
5. 现场容错：断网、识别失败、同步失败、空名单和无数据时，都必须有明确反馈和可恢复路径。
6. 参数可配置：气瓶容量、满压、消耗率、提醒阈值和报警阈值不写死在视觉层。
7. 先看板、后操作：看板负责发现问题，详情页和语音页负责完成操作。

## 2. 页面与导航架构

### 2.1 页面清单

| 页面 | 页面标识 | 层级 | 核心作用 |
|---|---|---|---|
| 火场安全管控看板 | `BoardPage` | 一级页面 | 实时监控在场人员及气瓶倒计时 |
| 语音录入 | `VoiceEntryPage` | 一级页面 | 通过按住说话完成进场、出场登记 |
| 设置 | `SettingsPage` | 一级页面 | 配置服务器、计算参数和提醒方式 |
| 名单与热词 | `RosterPage` | 设置二级页面 | 管理消防员姓名和专业术语 |
| 人员详情 | `PersonnelDetailPage` | 看板二级页面 | 查看单个人员信息并完成出场登记 |

### 2.2 底部导航

为保持已确定的三 Tab 视觉方案，底部导航固定为三个一级入口：

| 位置 | 入口 | 交互说明 |
|---|---|---|
| 左侧 | 看板 | 返回实时监控主界面 |
| 中间 | 语音录入 | 使用凸起的大号圆形麦克风按钮，作为主要操作 |
| 右侧 | 设置 | 进入服务器、气瓶参数、提醒方式和名单管理 |

名单与热词不占用一级底部 Tab，放置于设置页面内的二级入口“名单与热词”。

### 2.3 页面跳转关系

```text
App启动
  ↓
BoardPage
  ├─ 点击人员卡片 → PersonnelDetailPage
  │                    └─ 确认出火场 → 返回BoardPage
  ├─ 底部语音按钮 → VoiceEntryPage
  │                    └─ 确认进场/出场 → 返回BoardPage
  └─ 设置 → SettingsPage
                └─ 名单与热词 → RosterPage
```

### 2.4 报警处置页面的处理方式

V0.1 暂不单独建立 `AlarmPage`。报警和超时作为看板卡片、人员详情页的状态变体处理：

- 看板：突出显示报警/超时人员，支持快速进入详情；
- 人员详情：显示完整气瓶信息、实时倒计时和出场登记操作；
- 后续如增加“确认报警、联系人员、通知指挥员、申请增援、关闭报警、处置留痕”等操作，再独立拆分报警处置页面。

## 3. 视觉基础规范

### 3.1 颜色系统

以下颜色为V0.1推荐值，进入Figma后应通过实际设备和对比度检查最终确认。

| Token | 色值 | 用途 |
|---|---|---|
| `color.background.primary` | `#F2F4F7` | 页面主背景 |
| `color.surface.primary` | `#FFFFFF` | 顶部概览、设置区域、弹窗 |
| `color.text.primary` | `#111111` | 主标题、姓名、倒计时、主要数据 |
| `color.text.secondary` | `#4B5563` | 辅助说明、单位、时间标签 |
| `color.text.tertiary` | `#6B7280` | 次要提示、来源说明、占位文本 |
| `color.border.default` | `#D7DDE5` | 输入框、分割线、未激活边框 |
| `color.surface.subtle` | `#E8ECF1` | 非重点信息背景、禁用区域 |
| `color.status.safe` | `#32C943` | 安全状态卡片、状态标签、进度环 |
| `color.status.caution` | `#FFB000` | 注意状态卡片、状态标签、进度环 |
| `color.status.alarm` | `#F5222D` | 报警状态卡片、报警边框和危险提示 |
| `color.status.timeout` | `#C90016` | 超时状态、极端危险提示 |
| `color.action.voice` | `#FF9500` | 中央语音按钮、主要录音动作 |
| `color.action.primary` | `#111111` | 常规确认按钮、主要文字按钮 |
| `color.action.inverse` | `#FFFFFF` | 红色卡片上的倒计时和按钮文字 |
| `color.connection.online` | `#20A83A` | 云端连接正常 |
| `color.connection.offline` | `#6B7280` | 云端断开或未连接 |

#### 颜色使用规则

1. 绿色、橙黄色、红色只承担状态语义，不用于普通装饰。
2. 安全卡片建议使用黑色文字，报警卡片使用白色文字和白色倒计时。
3. 报警与超时不能只依靠颜色表达，必须同时显示“报警”或“超时”文字。
4. 超时状态优先使用深红色倒计时，并保留红色卡片背景和明确文字。
5. 红色呼吸闪烁只用于报警/超时危险条目，普通页面不得使用持续闪烁。
6. 语音按钮使用橙色，与红色报警状态保持区分，避免用户误把录音按钮理解为报警按钮。

### 3.2 字体

#### 中文字体

优先使用：

```text
PingFang SC, Noto Sans CJK SC, Microsoft YaHei, sans-serif
```

#### 数字字体

倒计时和压力数值优先使用等宽或具备等宽数字特性的字体：

```text
DIN Alternate, SF Mono, Roboto Mono, ui-monospace, monospace
```

数字必须使用等宽数字，避免倒计时变化时页面发生横向跳动。

#### 字级层级

| Token | 建议字号 | 行高 | 字重 | 主要用途 |
|---|---:|---:|---:|---|
| `type.display.xl` | 56–72px | 1.0 | 700 | 看板卡片主倒计时 |
| `type.display.lg` | 40–48px | 1.05 | 700 | 顶部最早到期、详情页倒计时 |
| `type.heading.1` | 24–28px | 1.2 | 700 | 页面标题 |
| `type.heading.2` | 20–24px | 1.25 | 700 | 人员姓名、模块标题 |
| `type.body.lg` | 16–18px | 1.4 | 600 | 状态标签、主要操作文字 |
| `type.body.md` | 14–16px | 1.45 | 500 | 常规正文、字段值 |
| `type.label` | 12–14px | 1.35 | 600 | 单位、字段标题、来源说明 |
| `type.caption` | 11–12px | 1.4 | 500 | 辅助提示、同步时间 |

#### 字体规则

1. 页面标题和人员姓名不得使用细体。
2. 倒计时优先使用粗体或中粗体，不使用过细数字。
3. 重要信息不得小于14px；报警、超时和主要按钮文字不得小于16px。
4. 文字尽量使用短句和短标签，避免现场阅读长段落。

### 3.3 间距系统

采用8px基础间距系统：

| Token | 数值 | 用途 |
|---|---:|---|
| `space.1` | 4px | 图标与文字微间距 |
| `space.2` | 8px | 标签内部间距、紧邻元素 |
| `space.3` | 12px | 卡片内部小间距、列表间距 |
| `space.4` | 16px | 页面左右边距、普通组件间距 |
| `space.5` | 20px | 卡片内边距、模块间距 |
| `space.6` | 24px | 区块间距、页面标题下间距 |
| `space.8` | 32px | 页面主要区域间距 |
| `space.10` | 40px | 重要内容之间的留白 |

推荐页面左右边距为16px，空间充足的设备可增加至20px，但不得导致核心倒计时过度压缩。

### 3.4 圆角与阴影

| Token | 数值 | 用途 |
|---|---:|---|
| `radius.sm` | 8px | 小标签、输入框内部元素 |
| `radius.md` | 14px | 普通容器、设置项 |
| `radius.lg` | 20px | 人员卡片、顶部概览卡片 |
| `radius.xl` | 28px | 语音按钮、重点浮层 |
| `radius.pill` | 999px | 状态徽章、开关、分段Tab |

阴影保持克制：

```css
shadow.card: 0 2px 8px rgba(17, 17, 17, 0.08)
shadow.float: 0 6px 16px rgba(17, 17, 17, 0.16)
```

不使用长投影、霓虹光、玻璃拟态和复杂渐变。

### 3.5 触控和安全区域

| 项目 | 最低要求 | 推荐要求 |
|---|---:|---:|
| 普通可点击区域 | 48×48px | 56×56px |
| 关键操作按钮 | 56×56px | 64×64px |
| 中央语音按钮 | 72×72px | 80–88px |
| 卡片之间最小间距 | 8px | 12–16px |
| 底部导航有效高度 | 72px | 80–96px |

底部导航和语音按钮必须避开系统手势区域。语音按钮不得遮挡人员卡片中的倒计时或其他可点击内容。

## 4. 核心组件规范

### 4.1 顶部概览横幅 SummaryBanner

用于看板顶部，显示整体态势，不承担复杂操作。

内容建议：

1. 在场人数：`在场 12人`；
2. 需关注人数：`需关注 3人`；
3. 最早到期：`最早到期 00:38`；
4. 右上角显示云端连接状态和最近同步时间。

布局规则：

- 使用白色大圆角卡片；
- 三个数据区域横向排列，使用细分割线区分；
- “最早到期”数值根据最危险状态显示橙色、红色或深红色；
- 在场人数和需关注人数使用黑色大字号；
- 不在概览横幅内放置“确认”“删除”等高风险操作。

### 4.2 人员状态卡片 PersonnelCard

人员卡片是看板核心组件。

#### 标准结构

```text
人员姓名          状态标签
气瓶压力 / 时长上限
剩余倒计时
```

建议字段：

- 姓名；
- 气瓶压力，例如 `28.5 MPa`；
- 时长上限，例如 `45 min`；
- 剩余倒计时，例如 `08:42`；
- 状态标签：安全、注意、报警、超时；
- 可选信息：进场时间、数据更新时间。

#### 状态样式

| 状态 | 卡片背景 | 主要文字 | 倒计时 | 辅助表达 |
|---|---|---|---|---|
| 安全 | 饱和绿色 | 黑色 | 黑色 | 绿色状态标签 |
| 注意 | 饱和橙黄色 | 黑色 | 黑色 | 橙黄色状态标签 |
| 报警 | 饱和红色 | 白色 | 白色 | 红色状态标签或报警图标 |
| 超时 | 深红色 | 白色 | 白色 | “超时”标签、倒计时停止或显示负值规则 |

#### 卡片规则

1. 卡片最小高度建议为120px，具体高度根据实际屏幕和字段数量调整。
2. 倒计时是卡片视觉重心，字号不得小于56px。
3. 姓名和状态标签必须位于卡片上方或同一水平层级，不能被次要字段淹没。
4. 关键状态信息不得只放在卡片右上角的小标签内。
5. 报警和超时卡片使用红色呼吸边缘或轻微闪烁，静态状态仍必须清晰可读。
6. 卡片整体可点击，点击区域覆盖整张卡片，不要求用户点击姓名或小图标。

### 4.3 状态标签 StatusBadge

状态标签采用圆角胶囊形，包含文字和可选图标。

| 状态 | 标签文字 | 推荐图标 |
|---|---|---|
| 安全 | 安全 | 对勾或盾牌 |
| 注意 | 注意 | 感叹号 |
| 报警 | 报警 | 警铃或三角警告 |
| 超时 | 超时 | 闹钟或警告 |

状态标签必须同时具备文字和颜色。图标用于加速识别，不替代文字。

### 4.4 倒计时 Countdown

#### 显示规则

- 常规显示：`MM:SS`；
- 超过60分钟时：根据实际业务决定显示`H:MM:SS`或分钟整数；
- 进入报警阈值后使用红色状态卡片；
- 超时后显示“超时”文字，并按照业务规则显示`00:00`、负值或“已超时”；
- 倒计时更新不改变数字区域宽度；
- 在详情页同时显示剩余时间和预计到期时间。

#### 动效规则

- 正常倒计时：无闪烁，只进行数字变化；
- 报警倒计时：可使用低频呼吸变化；
- 超时倒计时：可使用红色呼吸变化；
- 不使用快速高频闪烁，避免造成视觉疲劳和现场误判；
- 动画关闭或系统减少动效时，必须保留静态红色边框、图标和文字。

### 4.5 中央语音按钮 VoiceButton

语音按钮是一级导航中最重要的操作入口。

#### 视觉

- 使用强调色`#FF9500`；
- 圆形、凸起、明显高于底部导航栏；
- 直径建议80–88px；
- 使用白色或近黑色麦克风图标，确保对比度；
- 按钮周围保留足够空白，避免与两侧导航项混淆。

#### 状态

| 状态 | 视觉 | 操作反馈 |
|---|---|---|
| 默认 | 橙色圆形按钮 | 显示麦克风图标 |
| 按下 | 颜色加深或轻微缩放 | 显示“松开结束” |
| 录音中 | 橙色按钮加外圈脉冲 | 显示录音时长或音量反馈 |
| 识别中 | 保持按钮位置，显示加载态 | 禁止重复触发 |
| 失败 | 保持可操作，显示错误提示 | 支持重新录音 |
| 无权限 | 显示权限说明 | 引导开启麦克风权限 |

### 4.6 底部导航 BottomNavigation

底部导航固定包含三个入口：看板、语音录入、设置。中间语音按钮采用悬浮方式突出显示。

规则：

1. 看板和设置使用常规图标+文字；
2. 中间语音按钮使用圆形按钮，不再重复显示普通Tab背景；
3. 当前Tab使用黑色高对比图标和文字；
4. 非当前Tab使用`#6B7280`，但对比度不得过低；
5. 底部导航不得加入第四个一级入口；
6. 语音按钮按住录音，点击进入语音录入页面时，应有明确的进入反馈。

### 4.7 连接状态 ConnectionStatus

连接状态需要在看板、语音录入和设置页面保持一致。

| 状态 | 图标 | 文案 | 处理方式 |
|---|---|---|---|
| 正常 | 绿色实心点或云端图标 | 已连接 | 可正常同步和识别 |
| 同步中 | 转圈图标 | 同步中 | 禁止重复触发同步 |
| 断开 | 灰色云端断开图标 | 云端断开 | 提示检查服务器或网络 |
| 同步失败 | 警告图标 | 同步失败 | 提供重试入口 |

连接状态不能只显示在设置页。看板和语音录入页必须让用户知道当前数据是否为实时数据。

### 4.8 SnackBar、对话框和空状态

#### SnackBar

用于短暂反馈，不承载关键报警：

- `设置已保存`；
- `添加成功`；
- `删除成功`；
- `同步失败，请重试`；
- `录入成功`。

#### 二次确认对话框

用于确认人员出火场、删除名单、删除专业术语等不可逆或影响数据的操作。

对话框必须包含：

- 明确标题；
- 简短说明；
- 取消按钮；
- 主要确认按钮；
- 对危险操作使用红色确认按钮，但不使用大面积红色背景。

#### 空状态

空状态要说明“当前没有什么”和“接下来可以做什么”。

示例：

- `暂无人员在场`——可通过语音录入完成进场登记；
- `暂无消防员`——添加姓名后可提升语音识别准确率；
- `暂无专业术语`——添加常用术语作为识别热词。

## 5. 页面设计规范

### 5.1 BoardPage：火场安全管控看板

### 页面目标

实时监控在场人员及其气瓶倒计时，按安全、注意、报警、超时进行分级，是App启动后的默认页面。

### 页面结构

```text
页面标题与连接状态
顶部概览横幅
人员卡片列表
底部三项导航
```

### 信息层级

1. 第一层：报警/超时人员、最早到期时间；
2. 第二层：在场人数、需关注人数；
3. 第三层：单个人员的姓名、压力和时长上限；
4. 第四层：同步时间、数据来源等辅助信息。

### 状态

#### 默认状态

显示实时人员卡片列表，按照危险程度排序：

1. 超时；
2. 报警；
3. 注意；
4. 安全。

同一状态内可按照剩余时间从少到多排序。

#### 同步中

- 顶部连接状态显示“同步中”；
- 卡片保持现有数据，不整体替换为空白骨架；
- 首次启动没有数据时可使用骨架屏；
- 同步过程中禁止显示“实时”字样，避免造成误解。

#### 同步失败

- 顶部显示云端断开或同步失败图标；
- 显示最近一次成功同步时间；
- 提供“重试同步”操作；
- 不隐藏已有数据，但明确标识数据可能不是最新状态。

#### 空状态

- 显示`暂无人员在场`；
- 提供进入语音录入的引导；
- 不显示无意义的空卡片。

#### 报警/超时

- 卡片使用红色或深红色整块背景；
- 倒计时使用白色大字号；
- 显示明确的“报警”或“超时”文字；
- 允许通过点击整张卡片进入人员详情。

### 交互

- 点击人员卡片进入`PersonnelDetailPage`；
- 下拉刷新或点击重试触发同步；
- 点击中央语音按钮进入`VoiceEntryPage`；
- 不在看板中加入复杂编辑功能，避免破坏监控连续性。

### 5.2 VoiceEntryPage：语音录入

### 页面目标

通过按住说话完成进场或出场登记，自动识别姓名、气瓶压力并计算可用时间。

### 页面结构

```text
页面标题与连接状态
录音操作区
识别转写卡片
解析结果卡片
计算结果与公式说明
确认操作区
底部三项导航
```

### 录音交互

1. 默认状态显示大号语音按钮和“按住说话”；
2. 按住按钮进入录音状态；
3. 松手后自动进入识别状态；
4. 识别完成后显示原始转写文本和解析结果；
5. 用户可以编辑姓名和压力；
6. 用户确认后开始进场倒计时或登记出场。

### 解析结果

需要明确显示：

- 指令类型：进场或出场；
- 姓名；
- 当前气瓶压力；
- 计算出的可用时间；
- 当前连接状态；
- 原始语音转写内容。

### 计算说明

界面显示“可用时间”及公式说明，但不在UI层固定具体公式。计算结果由气瓶容量、满压、当前压力、消耗率、提醒阈值和报警阈值共同决定，具体参数由设置页面配置或由服务端下发。

### 错误状态

| 状态 | 提示 | 处理方式 |
|---|---|---|
| 无麦克风权限 | 需要开启麦克风权限 | 提供前往系统设置入口 |
| 录音启动失败 | 无法开始录音 | 提供重试 |
| 未识别到语音 | 未听清，请重新说 | 保留重新录音按钮 |
| 识别失败 | 语音识别失败 | 支持手动填写 |
| 未找到在场人员 | 未找到对应人员 | 提示检查姓名或返回看板 |
| 姓名未填写 | 请填写姓名 | 定位到姓名输入框 |
| 压力异常 | 请输入0–40 MPa范围内的数值 | 高亮压力字段 |
| 云端断开 | 当前无法完成云端识别 | 提供重试和连接检查 |

0 MPa 是否允许作为有效进场数据，需要在业务规则中最终确认；UI应支持边界值单独提示。

### 5.3 PersonnelDetailPage：人员详情

### 页面目标

查看单个在场人员的完整信息、实时倒计时和气瓶参数，并完成出场登记。

### 页面结构

```text
返回按钮与人员姓名
状态色块与状态标签
环形倒计时进度
气瓶信息
进场/预计出场信息
来源与原始语音转写
确认出火场按钮
```

### 视觉规则

- 顶部使用大面积状态色块或状态横幅；
- 环形倒计时使用对应状态色；
- 倒计时中心显示剩余时间，外侧显示预计到期时间；
- 报警和超时仍使用红色状态，并增加文字和图标说明；
- 环形进度不能作为唯一状态表达方式。

### 关键操作

“确认已出火场”是页面主要操作，必须满足：

1. 使用足够大的按钮；
2. 点击后弹出二次确认对话框；
3. 确认成功后播报语音；
4. 返回看板并刷新人员数量和关注人数；
5. 记录实际出场时间和操作来源。

### 异常状态

- 该人员已出火场：显示已出火场空状态，不显示可重复登记按钮；
- 记录不存在：显示记录已失效，并提供返回看板入口；
- 报警/超时：保留出场登记操作，同时突出危险状态；
- 数据刷新失败：保留最近数据，显示最后同步时间。

### 5.4 SettingsPage：设置

### 页面目标

配置服务器连接、场景信息、访问令牌、气瓶计算参数和提醒方式。

### 页面分区

1. 服务器连接
   - 服务器地址；
   - 场景码；
   - 访问令牌；
   - 当前连接状态。
2. 气瓶计算参数
   - 气瓶容量；
   - 满压；
   - 消耗率；
   - 提醒阈值；
   - 报警阈值。
3. 提醒方式
   - 语音播报开关；
   - 报警音开关。
4. 名单与热词
   - 进入`RosterPage`。

### 表单规则

- 访问令牌默认隐藏，支持显示/隐藏；
- 压力、容量、消耗率和阈值必须显示单位；
- 数值字段支持键盘快速输入和边界校验；
- 保存按钮在页面底部保持可见，但不遮挡底部导航；
- 保存后刷新配置并重启同步，必须显示同步过程；
- 未保存修改离开页面时，提示是否保存。

### 状态

除“设置已保存”外，还应定义：

- 保存中；
- 保存失败；
- 参数校验失败；
- 服务器连接失败；
- 配置刷新中；
- 配置刷新完成。

### 5.5 RosterPage：名单与热词

### 页面目标

管理消防员姓名和专业术语，作为语音识别热词，提升现场识别准确率。

### 页面结构

```text
返回按钮与页面标题
消防员 / 专业术语分段Tab
数量徽章
输入框与添加按钮
条目列表
删除操作
```

### 交互规则

- “消防员”和“专业术语”使用两个内部Tab切换；
- Tab上显示当前条目数量；
- 添加后立即显示在列表顶部或按字母/拼音排序，排序规则需统一；
- 删除前必须二次确认；
- 删除失败显示SnackBar，不直接移除本地条目；
- 接口异常时保留当前列表并显示重试入口。

### 空状态

- 消防员空状态：`暂无消防员`，说明添加姓名可提升识别准确率；
- 专业术语空状态：`暂无专业术语`，提供典型术语示例但不自动写入正式名单。

## 6. 数据字段与状态映射

### 6.1 人员记录字段

| 字段 | 展示名称 | 展示位置 | 说明 |
|---|---|---|---|
| `id` | — | 不直接展示 | 人员记录唯一标识 |
| `name` | 姓名 | 卡片、详情页 | 必填 |
| `pressureMpa` | 气瓶压力 | 卡片、详情页 | 单位MPa |
| `maxDurationMin` | 时长上限 | 卡片、详情页 | 单位分钟 |
| `remainingSeconds` | 剩余时间 | 卡片、详情页 | 实时倒计时 |
| `status` | 状态 | 状态标签 | safe/caution/alarm/timeout |
| `enteredAt` | 进场时间 | 详情页 | 时间戳 |
| `estimatedExitAt` | 预计出场时间 | 详情页 | 时间戳 |
| `source` | 来源 | 详情页 | 语音/手动 |
| `rawTranscript` | 原始语音转写 | 详情页、录入页 | 可折叠查看 |
| `lastSyncedAt` | 最近同步时间 | 看板、详情页 | 断网时必须保留 |

### 6.2 状态判定

状态由服务端或统一计算模块判定，UI只负责展示：

```text
remaining > cautionThreshold       → 安全
alarmThreshold < remaining ≤ cautionThreshold → 注意
0 < remaining ≤ alarmThreshold      → 报警
remaining ≤ 0                       → 超时
```

阈值命名和方向需与后端最终定义保持一致。若服务端采用其他判定方式，前端不得自行产生第二套状态逻辑。

### 6.3 关注人数

V0.1推荐定义：

```text
需关注人数 = 注意人数 + 报警人数 + 超时人数
```

如果后续业务要求仅统计报警和超时，应同步修改概览横幅、颜色说明和测试用例。

## 7. 动效和声音反馈

### 7.1 动效

只保留有明确功能价值的动效：

- 同步中：转圈；
- 录音中：麦克风外圈脉冲或音量反馈；
- 报警/超时：低频呼吸闪烁；
- 页面切换：短距离、快速过渡；
- 保存成功：轻量确认反馈。

避免页面整体移动、复杂转场、连续快速闪烁和装饰性粒子效果。

### 7.2 声音

- 语音播报开关控制系统播报；
- 报警音开关控制报警音；
- 报警音和普通操作音必须有明显区别；
- 声音关闭后，视觉报警仍必须完整存在；
- 语音播报内容应简短，例如“张伟，气瓶时间即将到期”。

## 8. 可访问性与现场可用性

1. 重要文字与背景保持高对比度，进入Figma后进行对比度检查。
2. 红绿色弱情况下，仍可通过状态文字、图标、卡片顺序和倒计时识别状态。
3. 主要按钮不依赖精细拖动、长列表小目标或双指操作。
4. 录音、确认出场、重试同步等关键操作必须支持单手完成。
5. 重要状态不得仅通过轻微色差表达。
6. 列表滚动时，危险卡片不能被底部语音按钮遮挡。
7. 现场强光下，尽量避免浅灰文字、细边框和低饱和状态色。
8. 在系统开启较大字体时，核心倒计时、状态标签和按钮文字不能被截断。
9. 报警状态关闭动效后，仍需保留静态红色、文字、图标和白色倒计时。

## 9. Figma落地规范

### 9.1 Figma页面结构

```text
00_Cover
01_Foundations
02_Components
03_Patterns
04_Screens
05_Prototype
99_Archive
```

### 9.2 Variables命名

```text
Color / Background / Primary
Color / Surface / Primary
Color / Text / Primary
Color / Status / Safe
Color / Status / Caution
Color / Status / Alarm
Color / Action / Voice
Spacing / 4
Spacing / 8
Spacing / 12
Spacing / 16
Spacing / 20
Spacing / 24
Radius / Small
Radius / Medium
Radius / Large
Radius / Pill
```

### 9.3 组件命名

```text
AppShell
BottomNavigation
VoiceButton
SummaryBanner
PersonnelCard
StatusBadge
Countdown
ConnectionStatus
EmptyState
LoadingState
ErrorState
ConfirmDialog
SnackBar
FormField
SegmentedTab
```

### 9.4 组件变体

人员卡片至少建立以下变体：

```text
PersonnelCard / Safe
PersonnelCard / Caution
PersonnelCard / Alarm
PersonnelCard / Timeout
PersonnelCard / Loading
PersonnelCard / Disabled
```

语音按钮至少建立以下变体：

```text
VoiceButton / Default
VoiceButton / Pressed
VoiceButton / Recording
VoiceButton / Recognizing
VoiceButton / Error
VoiceButton / PermissionDenied
```

### 9.5 推荐制作顺序

1. 先建立颜色、字号、间距、圆角和阴影变量；
2. 建立底部导航、语音按钮、状态标签、倒计时和人员卡片；
3. 先完成BoardPage；
4. 再完成VoiceEntryPage和PersonnelDetailPage；
5. 最后完成SettingsPage和RosterPage；
6. 建立报警、超时、断网、空状态和识别失败原型；
7. 使用实际数据长度检查姓名、倒计时和异常提示是否溢出。

## 10. 验收标准

### 10.1 视觉验收

- 全部页面使用同一套颜色变量；
- 页面背景统一为浅灰白方向；
- 主文字保持近黑色和高对比；
- 状态卡片颜色具有明确语义；
- 中央语音按钮在所有一级页面位置一致；
- 没有额外的第四个底部一级导航入口。

### 10.2 交互验收

- App启动默认进入看板；
- 点击人员卡片可以进入详情；
- 中央语音按钮可以完成按住录音和松手识别；
- 进场确认后看板人员数量和倒计时更新；
- 出场确认后人员从在场列表移除；
- 设置保存后显示保存状态并刷新同步；
- 断网、识别失败、空列表和记录不存在均有明确反馈；
- 报警和超时状态均能通过颜色、文字和倒计时识别。

### 10.3 现场操作验收

- 戴手套时可以准确点击语音按钮和人员卡片；
- 强光下仍能读取姓名、状态和倒计时；
- 三秒内可以找出最危险人员；
- 报警状态不会因闪烁导致文字不可读；
- 操作过程中不会因误触造成出场登记或删除名单。

## 11. 待确认事项

以下事项不影响先建立Figma视觉基础，但在进入开发前必须确认：

1. 实际使用终端的屏幕尺寸和系统安全区域；
2. 9:16设计稿对应的实际设备比例；
3. 气瓶可用时间的最终计算公式；
4. 0 MPa是否允许作为有效录入值；
5. 报警阈值、提醒阈值的具体默认值和单位；
6. 超时后的倒计时显示方式；
7. 断网时是否允许本地缓存进场/出场记录；
8. 语音播报和报警音的具体文案及音频策略；
9. 是否需要后续增加独立的报警处置、历史记录或任务复盘页面。

## 12. 版本记录

| 版本 | 日期 | 说明 |
|---|---|---|
| V0.1 | 2026-08-02 | 根据高可视亮色方案和已明确页面功能建立初版规范 |
